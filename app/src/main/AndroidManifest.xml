<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.muhammadv2.going_somewhere">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyB_ZLQO3YBMItwIiTfG1xeex4S1O7LMhkI" />

        <activity
            android:name=".ui.trips.TripsActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".model.data.TravelsProvider"
            android:authorities="com.muhammadv2.going_somewhere"
            android:enabled="true"
            android:exported="false" />

        <activity android:name=".ui.trips.addTrip.AddTripActivity" />
        <activity android:name=".ui.tripDetails.addPlace.AddPlaceActivity" />
        <activity android:name=".ui.tripDetails.TripDetailsActivity" />
    </application>

</manifest>